# Playground

<script setup>
import MtPlayground from '../.vitepress/theme/components/MtPlayground.vue'
</script>

Edit and preview Mote UI components right in the browser — your code compiles and runs instantly. The editor state is serialized into the URL hash, so you can share your example by copying the link.

::: tip Note
The sandbox loads `mote-ui@0.1.0` and its styles from a CDN, so an internet connection is required.
:::

<MtPlayground />
