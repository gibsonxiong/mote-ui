import { withInstall } from '../../utils/with-install'
import Progress from './progress.vue'

export const MtProgress = withInstall(Progress, 'MtProgress')
export default MtProgress

export * from './types'
