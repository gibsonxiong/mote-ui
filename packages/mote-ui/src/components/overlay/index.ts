import { withInstall } from '../../utils/with-install'
import Overlay from './overlay.vue'

export const MtOverlay = withInstall(Overlay, 'MtOverlay')
export default MtOverlay

export * from './types'