export interface MtEmptyProps {
  /** Custom image url; falls back to the built-in illustration */
  image?: string
  /** Image width; accepts px number or any CSS size string */
  imageSize?: number | string
  description?: string
}
